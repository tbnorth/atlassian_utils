# Essential for connection
ATL_HOST_CONFLUENCE = https://confluence.example.com
ATL_HOST_JIRA = https://jira.example.com
ATL_HOST_BITBUCKET = https://bitbucket.example.com
ATL_USER = auser
# better to leave this out and `read -s ATL_PASS; export ATL_PASS`
# ATL_PASS = apassword
# Use ATL_PASS = token:ef79cf85a8968aa to use a token, in which case ATL_USER is ignored

# Tokens differ between Confleunce, Jira and Bitbucket, so use
# ATL_PASS_JIRA, ATL_PASS_CONFLUENCE and ATL_PASS_BITBUCKET to set them.

# Used by some processes
ATL_TO_TAG = ISSKEY-3 ISSKEY-607 ISSKEY-89
ATL_ADD_TAG = NeedsRefinement
ATL_TARGET_TITLE = Some page title
ATL_PROJECT = MYPROJ
ATL_TARGET_RELEASE = MyProj 2.2.0 Release 
ATL_ISSUE_KEY = FOO-1057

# reqpages.py
# Page to use as template
ATL_CONF_TEMPLATE_ID = 350487045
# Page to use as parent for categories
ATL_CONF_REQ_TOP = 350486704
# FY financial year text
ATL_CONF_REQ_FY = FY24

# projmd.py
# Contractor hourly rate
ATL_CONTRACTOR_RATE = 200
# Percent contractor time for cost calc.
ATL_CONTRACTOR_PROP = api 0 ui 100

# vim: ft=config
